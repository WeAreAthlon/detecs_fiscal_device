enum SeparatingLine { none, dash, dashSpace, equal, text }

enum TextBold { none, bold }

enum TextItalic { none, italic }

enum TextHeight { normal, double, half }

enum TextUnderline { none, underline }

enum TextAlign { left, center, right }

enum DiscountType {
  none,
  surchargeByPercent,
  discountByPercent,
  surchargeBySum,
  discountBySum,
}
